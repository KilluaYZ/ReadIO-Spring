<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pool.readio.readiombg.mapper.ApplicableRolesMapper">
  <resultMap id="BaseResultMap" type="com.pool.readio.readiombg.model.ApplicableRoles">
    <result column="grantee" jdbcType="DISTINCT" property="grantee" />
    <result column="role_name" jdbcType="DISTINCT" property="roleName" />
    <result column="is_grantable" jdbcType="DISTINCT" property="isGrantable" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    grantee, role_name, is_grantable
  </sql>
  <select id="selectByExample" parameterType="com.pool.readio.readiombg.model.ApplicableRolesExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from applicable_roles
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <delete id="deleteByExample" parameterType="com.pool.readio.readiombg.model.ApplicableRolesExample">
    delete from applicable_roles
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="com.pool.readio.readiombg.model.ApplicableRoles">
    insert into applicable_roles (grantee, role_name, is_grantable
      )
    values (#{grantee,jdbcType=DISTINCT}, #{roleName,jdbcType=DISTINCT}, #{isGrantable,jdbcType=DISTINCT}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.pool.readio.readiombg.model.ApplicableRoles">
    insert into applicable_roles
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="grantee != null">
        grantee,
      </if>
      <if test="roleName != null">
        role_name,
      </if>
      <if test="isGrantable != null">
        is_grantable,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="grantee != null">
        #{grantee,jdbcType=DISTINCT},
      </if>
      <if test="roleName != null">
        #{roleName,jdbcType=DISTINCT},
      </if>
      <if test="isGrantable != null">
        #{isGrantable,jdbcType=DISTINCT},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="com.pool.readio.readiombg.model.ApplicableRolesExample" resultType="java.lang.Long">
    select count(*) from applicable_roles
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update applicable_roles
    <set>
      <if test="row.grantee != null">
        grantee = #{row.grantee,jdbcType=DISTINCT},
      </if>
      <if test="row.roleName != null">
        role_name = #{row.roleName,jdbcType=DISTINCT},
      </if>
      <if test="row.isGrantable != null">
        is_grantable = #{row.isGrantable,jdbcType=DISTINCT},
      </if>
    </set>
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update applicable_roles
    set grantee = #{row.grantee,jdbcType=DISTINCT},
      role_name = #{row.roleName,jdbcType=DISTINCT},
      is_grantable = #{row.isGrantable,jdbcType=DISTINCT}
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
</mapper>