# Nacos 配置中心：生产/容器环境
spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:8848
      config:
        server-addr: http://nacos-registry:8848
        file-extension: yaml
        refresh-enabled: true