# 敏感信息，部署前请用实际 base64 替换
# 生成: echo -n "yourpassword" | base64
apiVersion: v1
kind: Secret
metadata:
  name: readio-secret
  namespace: readio
type: Opaque
stringData:
  # 部署时可直接写明文，K8s 会做 base64
  SPRING_DATASOURCE_PASSWORD: "readio"
  SPRING_DATA_REDIS_PASSWORD: "readio"
