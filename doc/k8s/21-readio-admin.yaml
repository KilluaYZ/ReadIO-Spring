apiVersion: apps/v1
kind: Deployment
metadata:
  name: readio-admin
  namespace: readio
  labels:
    app: readio-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: readio-admin
  template:
    metadata:
      labels:
        app: readio-admin
    spec:
      containers:
        - name: readio-admin
          image: readio-admin:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: readio-config
            - secretRef:
                name: readio-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: readio-admin
  namespace: readio
spec:
  selector:
    app: readio-admin
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
